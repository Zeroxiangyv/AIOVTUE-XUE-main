# WebDAV 相册密码（所有 WebDAV 相册共用，仅服务端 API 使用）
# 地址与账号请在各相册 pages/gallery/{slug}/index.md 的 webdav 字段中配置
#
# 本地开发：复制为 .env.local 后执行 pnpm dev
# Netlify：Site settings → Environment variables → WEBDAV_PASSWORD
# Vercel：Project Settings → Environment Variables → WEBDAV_PASSWORD（Production/Preview 均勾选）→ 重新部署
# Cloudflare Pages：Settings → Environment variables → WEBDAV_PASSWORD（Production/Preview）→ 重新部署
WEBDAV_PASSWORD=your_password
